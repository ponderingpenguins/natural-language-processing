% from hyperparameter tuning (selecting for highest accuracy on the dev set)
% - [17:04:30] Best Linear SVM params: {'C': 1.0, 'loss': 'hinge'} (Dev Accuracy: 0.9165)
% - [17:03:58] Best Logistic Regression params: {'C': 1.0} (Dev Accuracy: 0.9155)

In this section, we present the results of our experiments on the test set for both the Linear SVM and Logistic Regression models. We report the classification reports (precision, recall, F1-score, and support) for each class, as well as the confusion matrices for both models. Additionally, we provide a comparison of the overall accuracy and macro-F1 scores for both models on the test set.

We hyperparameter tuned both models on the dev set, selecting the best parameters based on the highest accuracy. For the Linear SVM, the best parameters were C=1.0 and loss=`hinge', while for Logistic Regression, the best parameter was C=1.0. We then evaluated both models on the test set using these best parameters and report the results below.

In Table \ref{tab:linear_svm_test_classification_report}, we can see that the Linear SVM model achieved an overall accuracy of 0.92 and a macro-F1 score of 0.92 on the test set. The Logistic Regression model, as shown in the subsequent table (Table \ref{tab:logistic_regression_test_classification_report}), achieved an overall accuracy of 0.91 and a macro-F1 score of 0.91 on the test set.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
 & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} & \textbf{Support} \\
\midrule
1            & 0.94 & 0.90 & 0.92 & 1900 \\
2            & 0.95 & 0.98 & 0.97 & 1900 \\
3            & 0.89 & 0.88 & 0.89 & 1900 \\
4            & 0.89 & 0.90 & 0.90 & 1900 \\
\midrule
Accuracy     & \multicolumn{2}{c}{} & 0.92 & 7600 \\
Macro avg    & 0.92 & 0.92 & 0.92 & 7600 \\
Weighted avg & 0.92 & 0.92 & 0.92 & 7600 \\
\bottomrule
\end{tabular}
\caption{Classification report for Linear SVM on test set where classes are labeled as 1: world, 2: sports, 3: business, and 4: sci/tech.}
\label{tab:linear_svm_test_classification_report}
\end{table}


% logistic regression (test) classification report
\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\toprule
 & \textbf{Precision} & \textbf{Recall} & \textbf{F1-Score} & \textbf{Support} \\
\midrule
1            & 0.93 & 0.91 & 0.92 & 1900 \\
2            & 0.95 & 0.98 & 0.96 & 1900 \\
3            & 0.88 & 0.89 & 0.88 & 1900 \\
4            & 0.89 & 0.89 & 0.89 & 1900 \\
\midrule
Accuracy     & \multicolumn{2}{c}{} & 0.91 & 7600 \\
Macro avg    & 0.91 & 0.91 & 0.91 & 7600 \\
Weighted avg & 0.91 & 0.91 & 0.91 & 7600 \\
\bottomrule
\end{tabular}
\caption{Classification report for Logistic Regression on test set where classes are labeled as 1: world, 2: sports, 3: business, and 4: sci/tech.}
\label{tab:logistic_regression_test_classification_report}
\end{table}

In the confusion matrices (Figures \ref{fig:linear_svm_test_confusion_matrix} and \ref{fig:logistic_regression_test_confusion_matrix}), we can observe that both models have similar patterns of misclassification, with the most common confusion occurring between the sci/tech and business classes. This is reflected in the classification reports where the precision, recall, and F1-scores for these classes are lower compared to the sport and world classes. Overall, the Linear SVM model performs slightly better than the Logistic Regression model on both accuracy and macro-F1, but the difference is not very large (e.g., 0.01 in accuracy and macro-F1).

\begin{figure}[H]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../figures/linear_svm_test_confusion_matrix.png}
        \caption{Confusion Matrix for Linear SVM on Test Set}
        \label{fig:linear_svm_test_confusion_matrix}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth]{../figures/logistic_regression_test_confusion_matrix.png}
        \caption{Confusion Matrix for Logistic Regression on Test Set}
        \label{fig:logistic_regression_test_confusion_matrix}
    \end{minipage}
\end{figure}



Comparing the two models in Table \ref{tab:comparison_test_set}, we can see that the Linear SVM model has a slightly higher accuracy and macro-F1 score compared to the Logistic Regression model on the test set. However, both models perform quite well, with accuracies and macro-F1 scores above 0.90, indicating that the TF-IDF features are effective for this classification task.

% table of comparison of accuracy and macro-F1 for linear svm and logistic regression on the test set
\begin{table}[h]
\centering
\begin{tabular}{lcc}
\toprule
\textbf{Model} & \textbf{Accuracy} & \textbf{Macro-F1} \\
\midrule
Linear SVM & \textbf{0.92} & \textbf{0.92} \\
Logistic Regression & 0.91 & 0.91 \\
\bottomrule
\end{tabular}
\caption{Comparison of Accuracy and Macro-F1 for Linear SVM and Logistic Regression on Test Set}
\label{tab:comparison_test_set}
\end{table}

% mention that the linear SVM performs slightly better than logistic regression on both accuracy and macro-F1, but the difference is not very large (e.g., 0.01 in accuracy and macro-F1)

% they also perform the same type of errors, as seen in the confusion matrices (e.g., both models get confused between sci/tech and business, which is reflected in the confusion matrices where there are more misclassifications between these classes than between the others)

% mention that for both models, sport is the easiest class to classify (highest precision, recall, and F1-score), while sci/tech is the hardest (lowest precision, recall, and F1-score), which is also reflected in the confusion matrices where there are more misclassifications involving sci/tech than the other classes